# 复制一份到 .env 并填入真 key。.env 不会被提交。

# Planner / Writer / Storyboarder LLM(v0.2 minimal pipeline 用)
ANTHROPIC_API_KEY=

# RunningHub(图 / 视频资产生成,v0.3 启用)
RUNNINGHUB_API_KEY=

# 其它候选 LLM(未来可切;见 PLAN.md §9)
# GEMINI_API_KEY=
# OPENAI_API_KEY=
